const modulePrefix = "/user";

const api = {
  loginAndregister: `${modulePrefix}/login_register`,
  register: `${modulePrefix}/register`,
  tokenAuth: `${modulePrefix}/token_auth`
};

export { modulePrefix, api };
